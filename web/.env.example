# Google ドライブ連携の設定（任意）。
# この3つ（最低 CLIENT_ID と API_KEY）を設定して `web/.env` として保存すると、
# 資料アップロード画面に「Google ドライブから選択」ボタンが有効化される。
# 未設定の場合はボタンが「未設定」表示になり、PC からのファイル選択のみ使える。
#
# 取得手順は ../docs/google-drive-setup.md を参照。

VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_API_KEY=
VITE_GOOGLE_APP_ID=
